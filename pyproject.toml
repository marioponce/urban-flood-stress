[project]
name = "urban-flood-stress"
version = "0.1.0"
description = "Urban flood stress workflows for NYC street data and 311 requests."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Mario Alberto Ponce-Pacheco", email = "map10194@nyu.edu" }
]
license = { text = "MIT" }

dependencies = [
  "numpy",
  "pandas",
  "matplotlib",
  "geopandas",
  "shapely",
  "pyproj",
  "python-igraph",
  "sodapy",
  "pyarrow",
]

[project.optional-dependencies]
dev = [
  "pytest",
  "black",
  "ruff",
  "pre-commit",
]

docs = [
  "sphinx",
  "furo",
  "myst-parser",
]

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-q"

[tool.black]
line-length = 88

[tool.ruff]
line-length = 88
exclude = ["notebooks"]

[tool.ruff.lint]
select = ["E", "F", "I"]
